name: CI
on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:
    inputs:
      run_native:
        description: Run the optional native compatibility workflow
        type: boolean
        required: false
        default: false
      native_profile:
        description: Native profile; minimum and all include exploratory legacy testing; minimum-android is a compatibility alias
        type: choice
        default: current
        options: [current, minimum, minimum-android, all]
      native_platform:
        description: Native platform to run
        type: choice
        default: both
        options: [android, ios, both]
      native_candidate_manifest:
        description: Optional full native candidate manifest JSON for a coordinated canonical manual run
        type: string
        default: ''
jobs:
  test:
    name: Node.js ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        # os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 26
      - run: git config --global user.name "Github Actions"
      - run: git config --global user.email "actions@users.noreply.github.com"
      - run: npm ci
      - run: npx -y playwright install --with-deps chromium
        if: runner.os == 'Linux'
      - run: npm run prepublishOnly
      - run: npm run test
        if: runner.os == 'Linux'
      - run: npm run test:types
        if: runner.os == 'Windows'
      - run: npm run test:node
        if: runner.os == 'Windows'
      - run: npm run test:engines
  native-compatibility:
    name: Native compatibility
    if: github.event_name == 'workflow_dispatch' && inputs.run_native
    uses: ./.github/workflows/native.yml
    with:
      profile: ${{ inputs.native_profile || 'current' }}
      platform: ${{ inputs.native_platform || 'both' }}
      candidate_package: react-native-outside
      candidate_repository: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name || github.repository }}
      candidate_sha: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
      candidate_manifest: ${{ inputs.native_candidate_manifest }}
      fixture_repository: ${{ github.repository }}
      fixture_sha: ${{ github.sha }}
    permissions:
      contents: read
