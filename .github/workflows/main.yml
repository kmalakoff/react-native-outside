name: CI
on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:
jobs:
  candidate-packages:
    name: Build reviewed sibling packages
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 26
      - uses: actions/checkout@v7
        with:
          repository: kmalakoff/react-native-contains
          ref: 8802658aefe6b46f10a29492a5066efde21a5ede
          path: .tmp/candidates/react-native-contains
      - name: Pack react-native-contains
        working-directory: .tmp/candidates/react-native-contains
        run: |
          npm ci
          npm run build
          mkdir -p ../../packages
          npm pack --ignore-scripts --pack-destination ../../packages
      - uses: actions/checkout@v7
        with:
          repository: kmalakoff/react-native-event
          ref: 97a360882cb70f19aff044d138875de8616716f5
          path: .tmp/candidates/react-native-event
      - name: Pack react-native-event
        working-directory: .tmp/candidates/react-native-event
        run: |
          npm ci
          npm run build
          mkdir -p ../../packages
          npm pack --ignore-scripts --pack-destination ../../packages
      - uses: actions/checkout@v7
        with:
          repository: kmalakoff/react-ref-boundary
          ref: 4642641ace59f315f81115829eca2659ff5ce106
          path: .tmp/candidates/react-ref-boundary
      - name: Pack react-ref-boundary
        working-directory: .tmp/candidates/react-ref-boundary
        run: |
          npm ci
          npm run build
          mkdir -p ../../packages
          npm pack --ignore-scripts --pack-destination ../../packages
      - uses: actions/upload-artifact@v4
        with:
          name: reviewed-sibling-packages
          path: .tmp/packages/*.tgz
          if-no-files-found: error
          include-hidden-files: true
  test:
    needs: candidate-packages
    name: Node.js ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        # os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 26
      - run: git config --global user.name "Github Actions"
      - run: git config --global user.email "actions@users.noreply.github.com"
      - run: npm ci
      - uses: actions/download-artifact@v4
        with:
          name: reviewed-sibling-packages
          path: .tmp/packages
      - name: Install reviewed siblings
        run: npm install --ignore-scripts --no-save --package-lock=false ./.tmp/packages/react-native-contains-1.0.16.tgz ./.tmp/packages/react-native-event-1.0.15.tgz ./.tmp/packages/react-ref-boundary-1.0.14.tgz
      - run: npx -y playwright install --with-deps
      - run: npm run prepublishOnly
      - run: npm run test
      - run: npm run test:engines
  ios-native-smoke:
    needs: candidate-packages
    name: iOS native smoke (RN 0.87.1)
    runs-on: macos-26
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 26
      - run: npm ci
      - uses: actions/download-artifact@v4
        with:
          name: reviewed-sibling-packages
          path: .tmp/packages
      - name: Install reviewed siblings
        run: npm install --ignore-scripts --no-save --package-lock=false ./.tmp/packages/react-native-contains-1.0.16.tgz ./.tmp/packages/react-native-event-1.0.15.tgz ./.tmp/packages/react-ref-boundary-1.0.14.tgz
      - run: npm run build
      - name: Install fixture dependencies
        working-directory: examples/native-test-app
        run: npm ci
      - name: Install reviewed fixture siblings
        working-directory: examples/native-test-app
        run: npm install --ignore-scripts --no-save --package-lock=false ../../.tmp/packages/react-native-contains-1.0.16.tgz ../../.tmp/packages/react-native-event-1.0.15.tgz ../../.tmp/packages/react-ref-boundary-1.0.14.tgz
      - name: Validate fixture
        working-directory: examples/native-test-app
        run: npm run validate
      - name: Build iOS bundle
        working-directory: examples/native-test-app
        run: npm run build:ios
      - name: Install iOS dependencies
        working-directory: examples/native-test-app
        run: pod install --project-directory=ios
      - name: Build and install fixture
        working-directory: examples/native-test-app
        env:
          SIMULATOR_DEVICE: iPhone 17
          SIMULATOR_RUNTIME: com.apple.CoreSimulator.SimRuntime.iOS-26-5
        run: |
          DEVICE_INFO="$(
            xcrun simctl list devices available --json |
              SIMULATOR_DEVICE="$SIMULATOR_DEVICE" node -e '
                let input = "";
                process.stdin.setEncoding("utf8");
                process.stdin.on("data", chunk => { input += chunk; });
                process.stdin.on("end", () => {
                  const data = JSON.parse(input);
                  const device = (data.devices[process.env.SIMULATOR_RUNTIME] ?? []).find(candidate => candidate.name === process.env.SIMULATOR_DEVICE && candidate.isAvailable);
                  if (!device) {
                    console.error(`No available simulator named ${process.env.SIMULATOR_DEVICE} for ${process.env.SIMULATOR_RUNTIME}`);
                    process.exitCode = 1;
                    return;
                  }
                  process.stdout.write(`${device.udid}\t${device.state}`);
                });
              '
          )"
          IFS=$'\t' read -r DEVICE_UDID DEVICE_STATE <<< "$DEVICE_INFO"
          test -n "$DEVICE_UDID"
          echo "SIMULATOR_UDID=$DEVICE_UDID" >> "$GITHUB_ENV"
          if [ "$DEVICE_STATE" != "Booted" ]; then
            xcrun simctl boot "$DEVICE_UDID"
          fi
          xcrun simctl bootstatus "$DEVICE_UDID" -b
          npm run ios -- --no-packager --udid "$DEVICE_UDID"
      - name: Set up Java for Maestro
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - name: Install Maestro
        run: |
          export MAESTRO_VERSION=2.7.0
          curl -fsSL https://get.maestro.mobile.dev | bash
      - name: Run Maestro flow
        env:
          # Cold CI simulators can take longer than Maestro's two-minute default.
          MAESTRO_DRIVER_STARTUP_TIMEOUT: '600000'
        run: |
          export PATH="$HOME/.maestro/bin:$PATH"
          maestro --device "$SIMULATOR_UDID" test test/integration/maestro/native-outside.yaml
      - name: Collect iOS diagnostics
        if: failure()
        run: |
          mkdir -p .tmp
          xcrun simctl list devices
          xcrun simctl io "$SIMULATOR_UDID" screenshot .tmp/native-outside-ios.png || true
      - name: Upload iOS diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: maestro-ios-diagnostics
          path: |
            .tmp/native-outside-ios.png
            ~/.maestro/tests
            ~/Library/Logs/CoreSimulator
          include-hidden-files: true
  android-native-smoke:
    needs: candidate-packages
    name: Android native smoke (RN 0.87.1)
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 26
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - run: npm ci
      - uses: actions/download-artifact@v4
        with:
          name: reviewed-sibling-packages
          path: .tmp/packages
      - name: Install reviewed siblings
        run: npm install --ignore-scripts --no-save --package-lock=false ./.tmp/packages/react-native-contains-1.0.16.tgz ./.tmp/packages/react-native-event-1.0.15.tgz ./.tmp/packages/react-ref-boundary-1.0.14.tgz
      - run: npm run build
      - name: Install fixture dependencies
        working-directory: examples/native-test-app
        run: npm ci
      - name: Install reviewed fixture siblings
        working-directory: examples/native-test-app
        run: npm install --ignore-scripts --no-save --package-lock=false ../../.tmp/packages/react-native-contains-1.0.16.tgz ../../.tmp/packages/react-native-event-1.0.15.tgz ../../.tmp/packages/react-ref-boundary-1.0.14.tgz
      - name: Validate fixture
        working-directory: examples/native-test-app
        run: npm run validate
      - name: Build Android bundle
        working-directory: examples/native-test-app
        run: npm run build:android
      - name: Install Maestro
        run: |
          export MAESTRO_VERSION=2.7.0
          curl -fsSL https://get.maestro.mobile.dev | bash
      - name: Enable KVM for Android emulator
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: Run Android fixture and Maestro flow
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          target: google_apis
          arch: x86_64
          profile: pixel_2
          emulator-options: -no-window -gpu swiftshader_indirect -no-snapshot -noaudio -no-boot-anim
          disable-animations: true
          script: bash test/integration/run-android.sh
      - name: Upload Android diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: maestro-android-diagnostics
          path: |
            .tmp/android-devices.txt
            .tmp/android-properties.txt
            .tmp/android-logcat.txt
            ~/.maestro/tests
          include-hidden-files: true
